{
    "name": "hello-react",
    "version": "1.0.0",
    "scripts": {
        "client": "cd client && yarn start",
        "server": "nodmon server.js",
        "dev": "concurrently --kill-other-on-fail \"yarn server\" \"yarn client\""
    },
    "dependencies": {
        "body-parser": "^1.19.0",
        "express": "^4.17.1",
        "nodemon": "^1.19.1"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
